{
  "name": "terminal-server",
  "version": "1.0.0",
  "description": "Node.js server that executes commands on the terminal",
  "main": "server.js",
  "dependencies": {
    "express": "^4.17.1"
  },
  "scripts": {
    "start": "node server.js"
  }
}
